# PRD: Генератор SQL VIEW для 1С

**Версия:** 4.25  
**Дата:** 2026-03-26  
**Статус:** Стабильная  

---

## 1. Обзор продукта

### 1.1 Проблема

Базы данных 1С используют обфусцированные технические имена таблиц (`_Document653`, `_Reference37172`) и полей (`Fld37206`, `_Fld10028RRef`), а связи между таблицами хранятся в виде GUID (`binary(16)`) без явных foreign keys. Это делает прямую работу с данными 1С через SQL крайне трудоёмкой.

Аналитики данных тратят часы на ручное построение SQL-запросов: расшифровку технических имён, поиск связей, составление JOIN-деревьев. Каждый раз при работе с новой таблицей фактов процесс повторяется заново.

### 1.2 Решение

Инструмент, который автоматически:
1. Принимает на вход таблицу фактов Ф.
2. Расшифровывает техническую структуру через файл документации (.docx).
3. Оценивает правомерность выбора Ф как таблицы фактов.
4. Строит GUID-индекс и индекс связей для всей БД.
5. Исследует окрестность Ф (связанные таблицы измерений, табличные части) через BFS/DFS.
6. Эвристически фильтрует полезные таблицы и поля.
7. Генерирует SQL-запрос для получения плоской денормализованной таблицы ФФ.

### 1.3 Целевая аудитория

**Аналитики данных**, которые:
- Знают SQL и понимают реляционную модель.
- Работают с данными 1С через BI-инструменты (например, Apache Superset).
- Нуждаются в быстром построении денормализованных витрин данных.

---

## 2. Пайплайн обработки

Процесс разбит на последовательные этапы. Каждый этап имеет чётко определённый вход и выход.

```
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│  Этап 0          Этап 1          Этап 2          Этап 2.5               │
│  ┌─────────┐     ┌──────────┐    ┌──────────┐    ┌───────────────┐      │
│  │ Выбор   │────▶│ Парсинг  │───▶│ GUID-    │───▶│ Индекс связей │      │
│  │ таблицы │     │ .docx    │    │ индекс   │    │ (relationship │      │
│  │ Ф       │     │          │    │          │    │  index)       │      │
│  └─────────┘     └──────────┘    └──────────┘    └──────┬────────┘      │
│                                                         │               │
│                                                         ▼               │
│  Этап 2.7        Этап 3          Этап 4          Этап 5                 │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐           │
│  │ Оценка   │    │ Граф     │───▶│ Фильтра- │───▶│ Генерация│           │
│  │ таблицы  │    │ связей   │    │ ция      │    │ SQL      │           │
│  │ Ф        │    │ (BFS/DFS)│    │          │    │          │           │
│  └──────────┘    └──────────┘    └──────────┘    └──────────┘           │
│                                                       │                 │
│                                                       ▼                 │
│                                                  ФФ (плоская            │
│                                                  денормализованная      │
│                                                  таблица)              │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 3. Функциональные требования

### Этап 0: Выбор таблицы фактов Ф

**Вход:** Действие пользователя — выбор таблицы.  
**Выход:** Имя таблицы фактов Ф (техническое имя в БД).

| ID     | Требование | Приоритет |
|--------|-----------|-----------|
| E0-01  | Приложение предоставляет **выпадающий список** всех таблиц из БД. | MUST |
| E0-02  | Список содержит как техническое, так и человеческое название таблицы (если доступно из .docx). Формат: `Человеческое (Техническое)`. | MUST |
| E0-03  | Список поддерживает **фильтрацию/поиск** по тексту (и по техническому, и по человеческому имени). | MUST |
| E0-04  | Список может быть отфильтрован по **типу таблицы**: Document, Reference, AccumRg, InfoRg, VT, Enum и др. | MUST |
| E0-05  | Пользователь выбирает **одну** таблицу фактов. Для табличных частей пользователь указывает VT-таблицу напрямую. | MUST |
| E0-06  | По умолчанию список отсортирован по типу таблицы (Document → AccumRg → InfoRg → прочие). | SHOULD |

---

### Этап 1: Парсинг файла структуры (.docx)

**Вход:** Файл `Структура.docx` с описанием таблиц и полей.  
**Выход:** Маппинги `техническое_имя ↔ человеческое_имя` для таблиц и полей.

| ID     | Требование | Приоритет |
|--------|-----------|-----------|
| E1-01  | Парсинг файла `.docx` извлекает: маппинг таблиц, маппинг полей, типы таблиц (Справочник, Документ, РегистрНакопления и др.). | MUST |
| E1-02  | Результат парсинга кешируется в JSON-файл (`temp/structure_parsed.json`) для ускорения повторных запусков. | MUST |
| E1-03  | Пользователь может загрузить файл структуры тремя способами: файл по умолчанию, загрузка через UI, указание пути. | MUST |
| E1-04  | Парсер корректно обрабатывает таблицы с суффиксами (основная, VT-части) и нормализует имена (добавляет подчёркивание). | MUST |

---

### Этап 2: GUID-индекс

**Вход:** Все таблицы 1С в БД.  
**Выход:** Словарь `{GUID (bytes) → имя_таблицы}`.

| ID     | Требование | Приоритет |
|--------|-----------|-----------|
| E2-01  | Для каждой таблицы 1С считываются значения первичных ключей (`_IDRRef`) и сохраняются в индекс. | MUST |
| E2-02  | Индекс сохраняется на диск (JSON с base64-кодированием ключей) для каждой БД отдельно. | MUST |
| E2-03  | При загрузке проверяется соответствие индекса текущему подключению (host + database). | MUST |
| E2-04  | Индекс можно перестроить принудительно через UI. | MUST |
| E2-05  | Визуализация GUID-индекса: столбчатая диаграмма распределения GUID по типам таблиц. | SHOULD |

---

### Этап 2.5: Индекс связей (relationship index)

**Вход:** GUID-индекс + все таблицы 1С.  
**Выход:** Словарь `{таблица → {поле → целевая_таблица}}`.

| ID     | Требование | Приоритет |
|--------|-----------|-----------|
| E2.5-01 | Для каждой таблицы и каждого поля `binary(16)` определяется целевая таблица через GUID-индекс (TOP 1 ненулевое значение → поиск в индексе). | MUST |
| E2.5-02 | Индекс сохраняется на диск (JSON) для каждой БД отдельно. | MUST |
| E2.5-03 | Индекс используется для **быстрого поиска обратных связей** (`find_reverse_relationships`): какие таблицы ссылаются на данную. | MUST |
| E2.5-04 | При загрузке проверяется соответствие текущему подключению. | MUST |
| E2.5-05 | Индекс можно перестроить принудительно через UI. | MUST |

---

### Этап 2.7: Статистика кардинальности полей

**Вход:** Все таблицы 1С в БД.  
**Выход:** `{таблица → {поле → {distinct_count, is_junk, data_type, max_length}}}`.

| ID     | Требование | Приоритет |
|--------|-----------|-----------|
| E2.7-01 | Для каждого поля каждой таблицы вычисляется `COUNT(DISTINCT)` по выборке `TOP(N)` строк. | MUST |
| E2.7-02 | Поля с `distinct_count ≤ junk_threshold` помечаются как мусорные (`is_junk`). | MUST |
| E2.7-03 | Статистика сохраняется на диск и загружается при повторных запусках. | MUST |
| E2.7-04 | Используется при автоматической фильтрации связей (F-01). | MUST |

---

### Этап 3: Оценка правомерности выбора таблицы Ф

**Вход:** Таблица Ф + данные парсинга из Этапа 1 + метаданные из БД.  
**Выход:** Оценка (балл/категория) + предупреждения с объяснениями.

| ID     | Требование | Приоритет |
|--------|-----------|-----------|
| E3-01  | Система оценивает таблицу Ф по набору эвристических признаков и выдаёт **интегральную оценку**: «Хорошо подходит», «Может подойти», «Вероятно не подходит». | MUST |
| E3-02  | Каждый сработавший признак отображается как отдельное **предупреждение с объяснением** причины. | MUST |
| E3-03  | Оценка носит **рекомендательный** характер; пользователь может продолжить работу вне зависимости от результата. | MUST |
| E3-04  | **Массовая оценка «фактотабличности»** по кнопке для **всех таблиц текущего списка** в UI (тот же фильтр по типу и поиск, что и выбор таблицы фактов). Результаты **накопительно** сохраняются в `output/fact_assessments.json`: для каждой технической таблицы хранится **последняя** оценка, остальные записи файла сохраняются. Доступны **сводная таблица** в интерфейсе (метрики H-01…H-07, итог, вес), **сортировка** по колонкам, **избранное** (тот же словарь `favorites`, что у выбора таблицы; при смене чекбокса — запись в `output/ui_state.json` **без** принудительного `st.rerun()`, стабильный `key` у `st.data_editor`, чтобы не сбрасывать прокрутку страницы), **экспорт XLSX**, выбор таблицы из отчёта как новой таблицы фактов. Одиночная оценка выбранной таблицы в шапке секции **не изменяется**. | MUST |

#### Эвристические признаки оценки:

| ID     | Признак | Вес | Описание |
|--------|---------|-----|----------|
| H-01   | **Тип таблицы** | Сильный | `Document*`, `AccumRg*` → сильный положительный. `Reference*` → сильный отрицательный. `Enum` → очень сильный отрицательный. `VT` → положительный. |
| H-02   | **Наличие числовых/денежных полей** | Средний | Поля типа `numeric`, `decimal`, `float`, `money` → положительный признак. |
| H-03   | **Наличие полей-дат** | Средний | Поля типа `datetime`, `datetime2` → положительный признак. |
| H-04   | **Количество строк** | Сильный штраф при мало данных | Пороги: ≥10 000 строк → +2; 100–9 999 → 0; **<100 → вес −6** (компромисс: мало строк — сильный сигнал против факта, но остальные эвристики не перекрываются полностью). |
| H-05   | **Наличие табличных частей (VT)** | Сильный отрицательный | Если у таблицы есть дочерние VT-таблицы → сильный отрицательный сигнал. |
| H-06   | **Количество внешних ссылок** | Слабый | Много полей `binary(16)` → скорее факт. |
| H-07   | **Ссылки на Document** | Средний | Наличие ссылок на таблицы Document через GUID-индекс. |

---

### Этап 4: Исследование окрестности таблицы Ф (граф связей)

**Вход:** Таблица Ф + GUID-индекс + индекс связей.  
**Выход:** Набор связанных таблиц с аннотациями (направление, тип, глубина, длина пути).

| ID     | Требование | Приоритет |
|--------|-----------|-----------|
| E4-01  | Исследование проводится в **двух направлениях**: прямые связи (Ф → измерения, `↓`) и обратные связи (другие таблицы → Ф, `↑`). | MUST |
| E4-02  | Глубина исследования **настраивается** отдельно для каждого направления. По умолчанию: ↓3 ↑1. | MUST |
| E4-03  | Обход графа выполняется алгоритмом **BFS** с отслеживанием `steps_down` и `steps_up`. | MUST |
| E4-04  | **Табличные части** (VT-таблицы) обнаруживаются и подтягиваются автоматически как обратные связи. | MUST |
| E4-05  | Для каждой связи определяется **тип**: `forward` (прямая, N:1) или `reverse` (обратная, 1:N). | MUST |
| E4-06  | Обратные связи находятся через **индекс связей** (быстрый поиск по всей БД). | MUST |
| E4-07  | Результат представляется в виде **дерева** в интерфейсе с информацией: техническое имя, человеческое имя, тип связи, уровень (ур), длина пути (дп). | MUST |
| E4-08  | Граф связей **сохраняется** на диск (JSON) и может быть загружен повторно. | MUST |
| E4-09  | При загрузке сохранённого графа проверяется актуальность (хэш параметров). | SHOULD |
| E4-10  | **Визуализация** графа связей: сетевой граф (NetworkX + Matplotlib) с цветовой кодировкой по типу таблицы. | SHOULD |

#### Фильтрация графа (постобработка DFS):

| ID     | Требование | Приоритет |
|--------|-----------|-----------|
| E4-F01 | Накопительные `steps_down` по DFS-пути от корня не превышают `max_depth_down`, а `steps_up` не превышают `max_depth_up`. Общая длина пути (дп) ≤ `max_depth_down + max_depth_up`. | MUST |
| E4-F02 | Исключаются **возвратные рёбра**: два соседних ребра между одними и теми же таблицами в противоположных направлениях. | MUST |
| E4-F03 | Исключение возвратного ребра каскадно удаляет все ветви, которые были бы доступны только через это ребро. | MUST |
| E4-F04 | Фильтрация применяется как к свежепостроенным, так и к загруженным с диска графам. | MUST |
| E4-F05 | DFS не заходит повторно в уже пройденную таблицу в рамках текущей ветки (table-path guard), предотвращая циклические обходы и потерю узлов с обратными связями от корня. | MUST |
| E4-F06 | Нормализация имён таблиц при построении дерева `_children`: связи индексируются и по оригинальному, и по нормализованному имени parent, чтобы DFS из нормализованного корня находил все связи. | MUST |

#### UI-фильтры графа:

| ID     | Требование | Приоритет |
|--------|-----------|-----------|
| E4-U01 | Фильтр по **уровню** (ур): multiselect, по умолчанию выбрано всё. **Опции** — объединение значений `ур` по **всем** найденным вариантам простого пути от **корневой таблицы факта** до ребра (тот же граф, что для кодов пути до `filter_graph`, с лимитами глубины). В **свернутой шапке** узла и в первичных картах для SQL показывается `ур` **первого** варианта после сортировки вариантов. После **«Применить»**: связь остаётся в списке, только если **существует один вариант пути**, для которого `ур` входит в выбранный multiselect **и одновременно** `дп` этого же варианта входит в применённый multiselect «Длина пути» (нельзя взять `ур` из одного варианта и `дп` из другого). Список обновляется по клику **«Применить»**, не при каждом изменении черновика. Санитизация выбранных значений при смене набора опций (удаление устаревших чисел). | MUST |
| E4-U02 | Фильтр по **длине пути** (дп): multiselect, по умолчанию выбрано всё. **Опции** — объединение `дп` по всем вариантам пути от корня; семантика пары **(ур, дп)** при отборе — см. E4-U01. Кнопка **«Применить»** и санитизация — как у уровня. | MUST |
| E4-U03 | Фильтры работают по логике AND. | MUST |
| E4-U04 | При раскрытии узла отображается **полный путь** от корня: все рёбра с нумерацией, направлением (→/←), именами таблиц/полей и **накопительными** `(↓sd ↑su)` **в рамках выбранного варианта** простого пути. Если к ребру ведут **несколько** простых путей от корня — блоки **Путь1**, **Путь2**, … с подписью пары (ур., дп.) варианта; при одном варианте без дублирующего заголовка. | MUST |
| E4-U05 | **Поиск в полях связей**: текстовый фильтр (без учёта регистра) по человеческим/техническим именам таблиц, строкам рёбер **всех вариантов** пути от корня, человеческим именам полей. Фильтрация по узлам: если хотя бы одна связь узла совпадает — показывается весь узел. Работает по AND с остальными фильтрами. | MUST |
| E4-U06 | **Код пути**: multiselect по строкам **↓** / **↑**; в опциях только коды, встречающиеся у **хотя бы одной отображаемой** связи как последовательность направлений по **простому** пути **от корневой таблицы факта** (блок «Корневая таблица»), без дополнительных стартов обхода (например документа-родителя для ТЧ); таблица на пути не повторяется; пределы `max_depth_down` / `max_depth_up` и длина пути в рёбрах `≤` их сумме. **Перечисление** допустимых кодов строится по топологии графа **до** постобработки `filter_graph` (снимок `last_graph_before_filter` при построении; при загрузке с диска — список из JSON до повторного `filter_graph`); **список связей, подписи узлов и SQL** — по графу после `filter_graph`. **Порядок** списка «Связанные таблицы» и отступы дерева: DFS **только** от корневой таблицы факта по отфильтрованному графу (без отдельных корней для ТЧ и т.п.). Для фильтра: связь остаётся, если выбранный код **совпадает с одним из** допустимых кодов этой связи (не только с каноническим путём в подписи узла). **Пустой выбор** после «Применить» — фильтр по коду не действует. Кнопка **«Применить»** рядом с полем. Сочетание с остальными фильтрами по **AND**. При смене графа черновик и применённые значения сбрасываются. | MUST |

---

### Этап 5: Фильтрация таблиц и полей

**Вход:** Набор таблиц и полей из Этапа 4.  
**Выход:** Отфильтрованный набор таблиц и полей, готовый для SQL-генерации.

#### Автоматические эвристики фильтрации:

| ID     | Эвристика | Действие | Переключаемая |
|--------|-----------|----------|---------------|
| F-01   | **Связи с < 2 уникальными значениями** в поле связи | Отключить связь | Да |
| F-02   | **Системные/служебные поля 1С** (`_Version`, `_Marked`, `_Predefined`, `_PredefinedID`, `_Description`, `_Code`, `_ParentIDRRef`, `_Folder`) | Логика по контексту | Да |
| F-03   | **Поля `binary(16)`** | По умолчанию исключать из SELECT; при явном выборе пользователем — включать | Нет |
| F-04   | **Пустые или почти пустые таблицы** (< 2 строк) | Отключить связь | Да |
| F-05   | **Поля типа `image`, `varbinary(max)`** (BLOB-данные) | Исключить из SELECT | Нет |

#### Фильтрация полей внутри таблиц измерений:

| Включать | Исключать |
|----------|-----------|
| `Наименование` / `_Description` | `ВерсияДанных` / `_Version` |
| `Код` / `_Code` (если информативный) | `ПометкаУдаления` / `_Marked` |
| Реквизиты с человеческими названиями | `ПредопределенныйЭлемент` / `_PredefinedID` |
| Числовые реквизиты (метрики) | `ЭтоГруппа` / `_Folder` |
| Даты (если не техническая дата) | `РодительIDRRef` / `_ParentIDRRef` |

Для **таблицы фактов Ф** включаются все содержательные поля, за исключением системных.

#### Пользовательское управление:

| ID     | Требование | Приоритет |
|--------|-----------|-----------|
| F-06   | Пользователь может **включить/отключить** любую связь (таблицу) вручную. | MUST |
| F-07   | Пользователь может **включить/отключить** отдельные поля внутри таблицы. | MUST |
| F-08   | Пользователь может переключить **тип JOIN** для каждой связи (INNER / LEFT / RIGHT / FULL OUTER в T-SQL). По умолчанию для новых связей **INNER JOIN** и в мастере генератора, и в ручном режиме. | MUST |
| F-09   | Отключение родительской связи **каскадно** отключает все дочерние связи. | MUST |
| F-10   | Переключатель **«Показывать только выбранное»** скрывает отключённые связи. | SHOULD |

---

### Этап 6: Генерация SQL

**Вход:** Отфильтрованный набор таблиц и полей из Этапа 5.  
**Выход:** SQL-скрипт для создания плоской денормализованной таблицы ФФ.

| ID     | Требование | Приоритет |
|--------|-----------|-----------|
| E6-01  | Генерация в **двух форматах**: `CREATE OR ALTER VIEW` и чистый `SELECT`. | MUST |
| E6-02  | **Именование полей**: два стиля — `classic` (`Поле` / `Алиас_Поле`) и `dotted` (`Таблица.Поле`). | MUST |
| E6-03  | Поля таблицы фактов Ф именуются **без префикса таблицы**. | SHOULD |
| E6-04  | **Исправление искажённых дат** 1С: `datetime2(0)` с годом ≥ 3000 → вычитание 2000 лет. Настраивается. | MUST |
| E6-05  | SQL содержит **комментарии**: заголовок с метаданными и комментарии у каждого JOIN-блока. | SHOULD |
| E6-06  | Результат доступен для **скачивания** как `.sql` файл и **копирования** из интерфейса. | MUST |
| E6-07  | Результат **сохраняется** в указанный путь на диске. | MUST |
| E6-08  | **Транзитные JOIN**: промежуточные связи, не включённые пользователем явно, но необходимые для формирования полного пути к включённой таблице, автоматически добавляются как JOIN без полей в SELECT. | MUST |

---

## 4. Итеративный процесс и сохранение конфигурации

| ID     | Требование | Приоритет |
|--------|-----------|-----------|
| I-01   | Интерфейс поддерживает **итеративный цикл**: изменение настроек и перегенерация SQL без повторного построения графа. | MUST |
| I-02   | Конфигурация проекта **сохраняется** в JSON: таблица Ф, настройки связей, глубина, фильтрация. | MUST |
| I-03   | Сохранённую конфигурацию можно **загрузить** для повторного использования. | MUST |
| I-04   | При загрузке конфигурации система **проверяет** актуальность (существуют ли таблицы/поля в текущей БД). | SHOULD |
| I-05   | Графы связей сохраняются отдельно и могут быть загружены с автоматической постобработкой. | MUST |
| I-06   | Состояние UI (последняя сессия) автоматически сохраняется и восстанавливается. | SHOULD |
| I-07   | В JSON конфигурации (`cfg_*.json`) в `metadata` сохраняются **пользовательские поля**: название, имя VIEW, описание, теги (список строк), плюс существующие технические метаданные. | MUST |
| I-08   | **Сквозной поиск** по всем `cfg_*.json` и метаданным архива `sql_*.json` (без фильтра по текущему графу): строка запроса в секции конфигураций; результаты с загрузкой конфигурации из поиска. | SHOULD |
| I-09   | Метаданные архива SQL (`sql_*.json`) дополняются полями `description`, `tags`, при наличии — `view_name`, `config_name` для поиска и учёта. | SHOULD |
| I-10   | В режиме **сквозного поиска** (непустой запрос): у строк результатов «Конфигурации» и «SQL-результаты» — чекбоксы выбора; **два ZIP** — только отмеченные `cfg_*.json` и отмеченные пары `sql_*.json` + `.sql`; **массовое удаление** отмеченных с чекбоксом подтверждения и сбросом кэша метаданных после операции. | SHOULD |

---

## 5. Интерфейсы

### 5.1 Streamlit UI (основной)

Многостраничное приложение:
- **Генератор VIEW** — пошаговый мастер (рекомендуется).
- **Ручной режим** — полный контроль над параметрами.

Структура страницы «Генератор VIEW» (сверху вниз):

```
┌──────────────────────────────────────────────┐
│ 1. 🔌 Подключение к БД                       │
│   [автоматическая проверка из .env]           │
├──────────────────────────────────────────────┤
│ 2. 🗄️ GUID-индекс и индекс связей            │
│   [построение / загрузка / перестроение]      │
│   [визуализация GUID-индекса]                │
├──────────────────────────────────────────────┤
│ 3. 📊 Статистика кардинальности полей         │
│   [построение / загрузка]                    │
├──────────────────────────────────────────────┤
│ 4. 📂 Файл структуры                         │
│   [загрузка / путь по умолчанию]             │
├──────────────────────────────────────────────┤
│ 5. 📋 Выбор таблицы фактов Ф                 │
│   [выпадающий список с поиском]              │
│   [фильтр по типу таблицы]                   │
├──────────────────────────────────────────────┤
│ 6. ✅ Оценка таблицы Ф                       │
│   [карточка с оценкой и предупреждениями]    │
├──────────────────────────────────────────────┤
│ 7. ⚙️  Параметры                              │
│   [глубина ↓ / ↑]                            │
│   [исправлять даты: да/нет]                  │
│   [именование полей: Таблица.Поле / Алиас]   │
│   [подсказка (?) с описанием параметров]     │
├──────────────────────────────────────────────┤
│ 8. 🔗 Граф связей                             │
│   [сохранённые графы; построение BFS]        │
│   [визуализация сетевого графа]              │
├──────────────────────────────────────────────┤
│ 9. 📂 Конфигурации проекта                    │
│   [сквозной поиск по cfg / sql-архиву]       │
│   [при непустом поиске: чекбоксы, 2×ZIP, удал.]│
│   [список cfg: загрузка, метаданные, удаление]│
│   [фрагмент UI: быстрые ✏️ / 📊 / поиск]      │
├──────────────────────────────────────────────┤
│ 10. 🗂️ Настройка таблиц и связей              │
│   [подзаголовок «Фильтрация»: поиск, мусорн., │
│    только выбранное, ур/дп/код пути + Применить]│
│   [корневая таблица: expander — поля сразу]  │
│   [Связанные таблицы: пагинация, узлы]       │
│   [чекбоксы вкл/выкл, тип JOIN]             │
│   [полный путь: Путь1…N при нескольких       │
│    простых путях; (↓sd ↑su) по варианту]    │
│   [исключение полей]                         │
├──────────────────────────────────────────────┤
│ 11. 💾 Выбранная конфигурация                 │
│   [метаданные, сохранение cfg]               │
│   [визуализация графа: только поля+транзит]  │
├──────────────────────────────────────────────┤
│ 12. 🚀 Генерация SQL                          │
│   [VIEW / SELECT / оба; имя VIEW; архив sql] │
│   [результат + скачивание]                   │
├──────────────────────────────────────────────┤
│ 13. 📋 Результат SQL                          │
└──────────────────────────────────────────────┘
```

### 5.2 CLI (вспомогательный)

```bash
python generate_view.py <таблица_фактов> \
    --structure-file input/Структура.docx \
    --max-depth 5 \
    --output output/view.sql \
    --format view|select|both \
    --config config/table_config.json \
    --no-fix-dates
```

---

## 6. Технический стек

| Компонент | Технология |
|-----------|-----------|
| Язык | Python 3.10+ |
| UI | Streamlit (многостраничное приложение) |
| База данных | MS SQL Server (через `mssql-python`) |
| Парсинг документов | `python-docx` |
| Визуализация графов | `networkx` + `matplotlib` |
| Экспорт XLSX (сводная оценка фактов) | `openpyxl` |
| Конфигурация | `.env` файл + `os.getenv()` |

---

## 7. Архитектура модулей

```
app.py                         — Главная страница Streamlit

pages/
  1_🚀_Генератор_VIEW.py       — Пошаговый мастер (основной режим)
  2_🔧_Ручной_режим.py         — Ручной режим (полный контроль)

parsers/
  structure_parser.py           — Этап 1: парсинг .docx

db/
  structure_analyzer.py         — Метаданные БД, GUID-индекс, индекс связей, статистика полей

builders/
  relationship_builder.py       — Этап 4: BFS-граф связей, поиск обратных связей, фильтрация возвратов

analyzers/
  fact_table_assessor.py        — Этап 3: оценка таблицы фактов (H-01…H-07), словарь metrics для отчёта
  fact_assessment_store.py      — Накопление оценок в fact_assessments.json, bulk-оценка, экспорт XLSX
  field_filter.py               — Этап 5: эвристическая фильтрация полей

generators/
  view_generator.py             — Этап 6: генерация SQL VIEW / SELECT, транзитные JOIN

utils/
  db_connection.py              — Утилиты подключения к БД
  guid_index_visualizer.py      — Визуализация GUID-индекса и графа связей

config.py                       — Конфигурация (версия, подключение, пути)
generate_view.py                — CLI интерфейс
bump_version.py                 — Скрипт увеличения версии
```

---

## 8. Ключевые подсистемы

### 8.1 GUID-индекс

**Назначение:** Быстрое определение целевой таблицы по значению GUID из поля `binary(16)`.

- Строится путём сканирования PK-полей (`_IDRRef`) всех таблиц 1С.
- Хранится в памяти как `Dict[bytes, str]` и на диске как JSON с base64.
- Используется при построении графа связей и при оценке таблицы фактов (H-07).

### 8.2 Индекс связей (relationship index)

**Назначение:** Быстрый поиск обратных связей без тяжёлых SQL-запросов.

- Строится на основе GUID-индекса: для каждого поля `binary(16)` каждой таблицы берётся `TOP 1` ненулевое значение и ищется в GUID-индексе.
- Хранится как `Dict[str, Dict[str, str]]` — `{таблица → {поле → целевая_таблица}}`.
- Позволяет мгновенно найти все таблицы, ссылающиеся на заданную (обратные связи).

### 8.3 BFS-граф связей

**Назначение:** Построение дерева связей от таблицы фактов.

- BFS-обход с раздельным отслеживанием `steps_down` и `steps_up`.
- Прямые связи: поля `binary(16)` текущей таблицы → целевые таблицы.
- Обратные связи: другие таблицы, ссылающиеся на текущую (через индекс связей).
- Постобработка: DFS-фильтрация возвратных рёбер и ограничение глубины по накопительным sd/su.

### 8.4 Фильтрация возвратных рёбер

**Назначение:** Исключение циклических путей в графе.

- Возвратное ребро: два соседних ребра между одними и теми же таблицами в противоположных направлениях (A→B, затем B←A).
- Проверка выполняется по паре (source_table, target_table, direction), без учёта конкретного поля.
- Удаление возвратного ребра каскадно удаляет все ветви, доступные только через него.

### 8.5 Накопительные sd/su и нормализация имён

**Назначение:** Корректный расчёт уровня и длины пути по DFS-дереву.

- DFS отслеживает `cumulative_sd` (шаги вниз) и `cumulative_su` (шаги вверх) по пути от корня.
- Ребро пропускается, если `sd > max_depth_down` или `su > max_depth_up`.
- Уровень = `sd − su`, длина пути = `sd + su`.
- Подписи `(↓x ↑y)` в UI показывают накопительные значения от корня, а не BFS-значения отдельного ребра.
- `_children` индексируется и по оригинальному, и по нормализованному имени parent (с `_` и без), чтобы DFS из нормализованного корня находил все связи (включая reverse от корня).
- Table-path guard предотвращает повторный заход в уже пройденную таблицу по текущей ветке DFS.

### 8.6 Транзитные JOIN и визуализация конфигурации

**Назначение (SQL):** Включение промежуточных связей в SQL для формирования полного пути.

- Если пользователь включил таблицу на глубине 2, но не включил промежуточную таблицу на глубине 1, система автоматически добавляет промежуточную связь как «транзитный» JOIN.
- Транзитные JOIN генерируют `JOIN` в SQL, но **не добавляют** поля в `SELECT`.

**Визуализация конфигурации (кнопка в секции 11)** — **уже**, чем полный набор JOIN в SQL:

- В граф попадают **только** узлы (таблицы), у которых есть **хотя бы одно выбранное поле**, и **транзитные** таблицы на **пути от корня** к таким таблицам (рёбра — объединение путей `gen_rel_key_to_path` для включённых связей с выбранными полями; без подмешивания всех связей графа).
- Связь «включена», но все поля сняты и узел не лежит на пути к таблице с полями — **не показывается**.
- Набор рёбер в картинке может отличаться от полного списка JOIN в SQL (SQL по-прежнему строится по правилам генератора VIEW).

### 8.7 Обратные связи (reverse) — JOIN и описание

**Назначение:** Корректная генерация JOIN и подсчёт полей для обратных связей (VT, регистры, другие таблицы, ссылающиеся на целевую).

- **Прямая связь (forward):** `ON [родитель].[field_name] = [потомок].[pk]` — FK в родителе, PK в потомке.
- **Обратная связь (reverse):** `ON [родитель].[pk] = [потомок].[field_name]` — PK в родителе, FK в потомке (поле связи в таблице, ссылающейся на целевую).
- При сохранении конфигурации и в описании «Таблицы и выбранные поля» для обратных связей используется **source** (таблица-источник), а не target.

### 8.8 Дефолтное состояние полей

**Назначение:** Согласованное начальное состояние при открытии формы или сбросе конфигурации.

- **Дефолт:** все поля не выбраны (все чекбоксы сняты).
- Применяется при: «Новая конфигурация (сброс)», построении графа, загрузке графа из «Сохранённые графы».
- При загрузке сохранённой конфигурации или восстановлении из last_session — восстанавливается сохранённое состояние.

---

## 8.9 История изменений

### v4.25 (2026-03-26)

| Изменение | Описание |
|-----------|----------|
| **PRD vs v4.24 (уточнение)** | В `pages/1_🚀_Генератор_VIEW` при смене «Избранное» в сводной таблице вызывается **только** `_save_ui_state` для `favorites`; **`st.rerun()` и `scroll_to_here` для этого сценария не применяются** — цель такого пути: не сбрасывать прокрутку страницы. У `st.data_editor` стабильный `key="gen_fact_bulk_editor"`. Ранее в истории v4.24 ошибочно было описано иначе. |

### v4.24 (2026-03-26)

| Изменение | Описание |
|-----------|----------|
| **Массовая таблица: избранное без rerun** | Сводная таблица: при изменении «Избранное» запись в `output/ui_state.json` без немедленного полного перезапуска скрипта; стабильный ключ виджета, чтобы уменьшить сброс прокрутки при работе с чекбоксом. *(Точное поведение уточнено в v4.25.)* |
| **Документация** | В §6 и §7 отражены `openpyxl` и модуль `fact_assessment_store.py`. |

### v4.23 (2026-03-26)

| Изменение | Описание |
|-----------|----------|
| **Сериализация metrics** | В `assessment_to_dict` добавлено поле **`metrics`** — массовая оценка сохраняет метрики в `fact_assessments.json`; колонки «М: …» в отчёте заполняются. В импорты добавлен **`Tuple`** (аннотация `FACT_METRIC_COLUMNS`). |
| **H-04** | Для `<100` строк вес **−6** (компромисс относительно −10: остальные эвристики не перекрываются полностью). |

### v4.22 (2026-03-26)

| Изменение | Описание |
|-----------|----------|
| **Метрики оценки** | `AssessmentResult.metrics`: сбор в тех же проходах `_assess_*`; целевая запись в JSON `result.metrics`; колонки «М: …» в UI и XLSX. Старые записи без `metrics` отображаются с пустыми «М:». (Сериализация в файл исправлена в v4.23.) |
| **H-04** | Ужесточение штрафа при `<100` строк относительно −1; точное значение веса — см. v4.23. |

### v4.21 (2026-03-26)

| Изменение | Описание |
|-----------|----------|
| **Массовая оценка (рефакторинг)** | Один проход `load_store` и `_load_ui_state` в expander; XLSX через `st.cache_data` по JSON строк; убрана прямая зависимость `pandas` из проекта (таблица — list of dict для `st.data_editor`). Прогресс-бар: минимум `1/total` при старте; отмена между таблицами (`should_cancel`); `hashlib.md5(..., usedforsecurity=False)` для ключа виджета. |
| **Документация** | В E3-04 явно указано: избранное хранится в `output/ui_state.json`. |

### v4.20 (2026-03-26)

| Изменение | Описание |
|-----------|----------|
| **Массовая оценка фактов** | В секции «6. Оценка таблицы фактов» добавлен expander: кнопка массового прогона `FactTableAssessor` по списку таблиц из п.5, прогресс, слияние в `output/fact_assessments.json` (схема `version` + `by_table`, поля `assessed_at`, `ok`, `result` или `error_message`). |
| **Сводка и экспорт** | Таблица в UI (`st.data_editor`): избранное синхронизируется с `ui_state.json`; экспорт накопленных строк в XLSX (`openpyxl`). Явная зависимость проекта: `openpyxl`. |
| **Выбор из отчёта** | Selectbox + кнопка установки таблицы фактов из успешных оценок (отложенный сброс фильтров п.5, как у избранного). |
| **Модуль** | `analyzers/fact_assessment_store.py` — сериализация, merge, bulk-оценка с продолжением при ошибке на одной таблице. |

### v4.19 (2026-03-26)

| Изменение | Описание |
|-----------|----------|
| **Навигация секции 10** | Цепочка «Перейти к настройке таблицы» / «Назад к …»: `st.session_state._nav_back_stack` (LIFO, лимит 32), миграция с `_nav_back_to_rk`. При смене графа (`gen_graph_hash` / блок сброса фильтров) стек навигации, `_nav_scroll_to_rk` и `_nav_arrived_at_rk` обнуляются. |
| **Фильтры multiselect** | У «Уровень», «Длина пути», «Код пути» убран `default=` при заданном `key` — значение только из `session_state`, без предупреждения Streamlit о двойной инициализации. |
| **Генератор SQL** | `logging.warning` при неизвестном `join_type` и в ветке `sorted_rels` (раньше только в рекурсивном построении JOIN). |
| **Репозиторий** | В `.gitignore` добавлен каталог `tmp/`. |

### v4.18 (2026-03-25)

| Изменение | Описание |
|-----------|----------|
| **Варианты путей (Путь1/Путь2)** | В `_collect_path_variants_from_fact_root` исходящие рёбра схлопываются по логическому ключу `(source_table, field_name, target_table, direction)`: у BFS-рёбер разные `relationship_key` из‑за `sd/su` в суффиксе, но одно физическое ребро. Представитель: `rk` из отфильтрованного графа `relationships`, иначе минимальная `depth`. Ключ DFS `_seen_key` — по визуальной сигнатуре пути; после сортировки — повторный дедуп списка вариантов по той же сигнатуре. `build_mixed_graph` / `filter_graph` / `_rels_for_path_codes` не менялись. |

### v4.17 (2026-03-24)

| Изменение | Описание |
|-----------|----------|
| **Секция 10: типы JOIN** | В «Связанные таблицы» доступны INNER / LEFT / RIGHT / **FULL OUTER JOIN**; дефолт для новых связей — INNER JOIN. `view_generator` принимает FULL OUTER в whitelist. Ручной режим: тот же набор опций и тот же дефолт INNER. Подсказки в UI и caption при выборе FULL OUTER о риске раздувания результата. |
| **Зависимости** | `streamlit>=1.37.0` (параметр `key` у `st.expander` для блока корневой таблицы). |
| **Корневой expander** | Инициализация через `setdefault`; по-прежнему `on_change="rerun"` (литерал API Streamlit для stateful expander). |
| **Генератор SQL** | При неизвестном `join_type` в конфиге — `logging.warning` и откат на INNER JOIN. |
| **Документация** | Удалён артефакт `tmp/plan-configMetadataAndSearch.md`; в `docs/plan-configMetadataAndSearch.md` убрана ссылка на tmp. |

### v4.16 (2026-03-24)

| Изменение | Описание |
|-----------|----------|
| **Порядок, варианты путей, фильтры ур/дп** | DFS списка «Связанные таблицы» и отступы дерева — только от **корневой таблицы факта**. Несколько простых путей от корня к одному ребру: в развёрнутом узле блоки **Путь1**, **Путь2**, … с парами (ур., дп.) и цепочками; в свёрнутой шапке — ур./дп. первого варианта и корректное склонение счётчика путей. Фильтры «Уровень» и «Длина пути» после «Применить»: связь проходит, если **существует один вариант**, у которого **и** `ур`, **и** `дп` попадают в применённые multiselect (без смешивания разных вариантов). Опции multiselect — **объединение** по всем вариантам; санитизация выбранных значений при смене графа/набора. Поиск по узлу — по цепочкам **всех** вариантов. |
| **RelationshipBuilder** | Очередь BFS в `build_mixed_graph`: пополнение по условию суммарной длины `(sd+su) ≤ max_down+max_up`; поле `last_graph_before_filter` — снимок рёбер до `filter_graph` для UI «Код пути». |
| **PRD** | E4-U01…U06 и макет секции 10 приведены к фактической семантике v4.16. |

### v4.15 (2026-03-24)

| Изменение | Описание |
|-----------|----------|
| **Коды пути: один старт** | Перечисление и фильтр «Код пути» — только простые пути от **корневой таблицы факта** (`selected_table_db`), без обхода с документа для ТЧ. Подпись узла и подсказки UI обновлены. E4-U06 уточнён. |

### v4.14 (2026-03-24)

| Изменение | Описание |
|-----------|----------|
| **Секция 10: коды пути в UI** | Под узлом связи — все коды и разбивка по корням (порядок корней = приоритет, как в DFS). Справка у multiselect «Код пути» и caption: фильтр — пересечение с объединением кодов связи; непересекающиеся multiselect не гарантируют непересекающиеся списки; «только один код ↓» фильтром не задаётся. |

### v4.13 (2026-03-24)

| Изменение | Описание |
|-----------|----------|
| **Перечисление кодов пути** | Код для ребра добавляется только если шаг не возвращает в уже посещённую таблицу (простой путь). Устранены ложные варианты вроде `↑↓` у связи «табличная часть → документ» при фильтре только `↑↓`. |

### v4.12 (2026-03-24)

| Изменение | Описание |
|-----------|----------|
| **BFS `build_mixed_graph`** | В очередь снова ставится вершина, если суммарная длина пути `(sd+su)` не превышает `max_depth_down + max_depth_up`, а не только при «запасе» по одной из осей — иначе не раскрывались прямые связи из таблиц, достигнутых последним шагом «вверх». |
| **Файл графа JSON** | Опционально `relationships_pre_filter` при сохранении; при загрузке источник для «Код пути» берётся из него, если есть. |

### v4.11 (2026-03-24)

| Изменение | Описание |
|-----------|----------|
| **Секция 10: источник рёбер для «Код пути»** | Перечисление кодов путей использует граф **до** `filter_graph` (`RelationshipBuilder.last_graph_before_filter` / сырой список из JSON при загрузке); в session state — `gen_relationships_path_codes_source`; сброс вместе с графом. Опции multiselect — только по кодам **отображаемых** связей. E4-U06 обновлён. |

### v4.10 (2026-03-26)

| Изменение | Описание |
|-----------|----------|
| **Секция 10: коды пути по всем простым путям (фаза A)** | Перечисление множества кодов ↓/↑ на связь без глобального `visited_rk`; опции multiselect — union по графу; фильтр — пересечение с множеством кодов связи; лимит 500000 шагов обхода; санитизация выбора по текущим опциям. E4-U06 уточнён. |

### v4.9 (2026-03-26)

| Изменение | Описание |
|-----------|----------|
| **Секция 10: порядок блоков** | Подзаголовок «Фильтрация» сразу под заголовком секции (поиск, мусорные поля, фильтры списка связей); затем корневая таблица, затем «Связанные таблицы». Ключи виджетов без изменений; видимость полей по мусорным — через `session_state.gen_show_junk_fields`. |

### v4.8 (2026-03-26)

| Изменение | Описание |
|-----------|----------|
| **Секция 10: отложенное применение фильтров** | У «Уровень», «Длина пути», «Код пути» — кнопки «Применить»; список связей строится по `*_applied` в session state; черновик в multiselect без пересчёта до клика. Миграция со старой сессии (копия в `*_applied`). |

### v4.7 (2026-03-25)

| Изменение | Описание |
|-----------|----------|
| **Секция 10: фильтр «Код пути»** | Multiselect по последовательностям ↓/↑ от корня (E4-U06); пустой выбор = без ограничения; сброс при смене графа (`gen_graph_hash`); порядок фильтрации: уровень + длина пути → код пути → «только выбранное» → поиск. |

### v4.6 (2026-03-24)

| Изменение | Описание |
|-----------|----------|
| **Массовые операции в поиске (секция 9)** | При непустом сквозном поиске: чекбоксы у строк конфигураций и SQL-результатов (ключи по хэшу `filepath`); кнопки «Скачать конфигурации (ZIP)» и «Скачать SQL-архив (ZIP)»; удаление выбранных с чекбоксом подтверждения; после удаления — `_invalidate_project_config_metadata_caches()` и rerun. |

### v4.5 (2026-03-23)

| Изменение | Описание |
|-----------|----------|
| **Корневая таблица (секция 10)** | Убрана промежуточная кнопка «Поля …» внутри expander корня: «Все»/«Ничего», фильтры типов и чекбоксы полей показываются сразу при раскрытии блока корневой таблицы (стрелка expander). Ключи виджетов `gen_f_*` / `gen_fa_*` / `gen_fn_*` без изменений. |

### v4.4 (2026-03-21)

| Изменение | Описание |
|-----------|----------|
| **Метаданные cfg / поиск** | В `metadata` cfg: name, view_name, description, tags; сквозной поиск по cfg и sql-архиву; кэш `@st.cache_data` для списков и инвалидация после save/delete. |
| **Секция «Конфигурации проекта» в `st.fragment`** | Заголовок 9, поиск и список cfg в фрагменте: клики ✏️/📊 не перезапускают весь скрипт (секция 10 «Настройка таблиц»). Полный `st.rerun()` для 📌/🗑️/💾 и «Новая конфигурация». |
| **Генерация SQL** | Передача `view_name` в генератор при валидном имени; дефолтный файл `.sql` и метаданные архива с description/tags/view_name/config_name. |
| **Визуализация конфигурации** | Граф только из таблиц с выбранными полями + транзит по путям от корня; убрано добавление всех связей вне DFS. Предупреждение, если нет ни одной таблицы с выбранными полями. |

### v4.3 (2026-03-18)

| Изменение | Описание |
|-----------|----------|
| **Производительность секции 10 (bulk enabled)** | Цикл по всем связям графа больше не вызывает `_get_table_columns_cached` на каждую связь при каждом рендере. Используется кеш `gen_rel_n_total` (rk → число колонок): `n_included = n_total − len(excluded_fields[rk])`. |
| **Персистентность кеша** | Поле `rel_n_total` в `last_session.json`; при «Новая конфигурация» и отложенной загрузке конфигурации кеш заполняется вместе с `excluded_fields`. |
| **Обратная совместимость** | Старые `last_session` без `rel_n_total`: дозаполнение кеша при первом рендере секции 10 по ключам из `excluded_fields` (однократные обращения к кешу колонок). |

### Предыдущие версии (v4.2, 2026-03-05)

| Изменение | Описание |
|-----------|----------|
| **Bug fix: потеря выбора полей при multi-target** | При поле с несколькими связанными таблицами (напр. `_Fld10123RRef` → `_Reference479` и `_Reference217`) выбор полей в ранее посещённых таблицах терялся при навигации «→». |
| **Решение: on_change callbacks** | `excluded_fields` — единственный источник правды. Обновление только через `on_change` при клике по чекбоксу. Widget keys используются только для отображения. |
| **Упрощение _sync_excluded_for_rel** | Убрано чтение widget keys; функция только инициализирует excluded_fields для новых rk и возвращает статистику. |
| **Удалена пересинхронизация в SQL-генераторе** | Блок «Синхронизация excluded_fields из чекбоксов» удалён — он перезатирал корректное состояние при отсутствии виджетов (другая страница, show_only_selected). |

### Предыдущие версии (v4.1, 2026-03-15)

| Изменение | Описание |
|-----------|----------|
| Обратные связи — JOIN | Для reverse: `ON [parent].[pk] = [child].[field_name]` (раньше ошибочно использовалось field_name на стороне родителя). |
| Обратные связи — описание | При сохранении конфигурации для reverse используется source (таблица-источник), а не target. |
| binary(16) при явном выборе | Поля `binary(16)` включаются в SELECT, если пользователь явно их выбрал (F-03 уточнён). |
| Дефолт «все не выбраны» | При «Новая конфигурация», построении/загрузке графа — все поля не выбраны. |

---

## 9. Границы и ограничения

| Ограничение | Описание |
|-------------|----------|
| Только 1C | Инструмент рассчитан только на базы данных 1С в формате MS SQL Server. |
| Один факт | Одно представление строится на основе одной таблицы фактов. |
| Без M:N | Связи типа «многие ко многим» не поддерживаются. |
| Без агрегаций | Генерируется плоская таблица без GROUP BY / агрегатных функций. |
| Без инкрементальности | Каждая генерация создаёт полное представление. |
| Эвристический подход | Определение связей основано на эвристиках (GUID-индекс, TOP 1), а не на гарантированных FK. |

---

## 10. Критерии успеха

1. Аналитик может получить готовый SQL VIEW за **< 5 минут** (вместо часов ручной работы).
2. Сгенерированный SQL **корректно выполняется** на целевой БД без ручных правок.
3. Имена полей в результирующей таблице **понятны** бизнес-пользователям.
4. Эвристики фильтрации **корректно** отсеивают > 80% нерелевантных полей без ручного вмешательства.
5. Конфигурация **переиспользуема**: сохранённый проект можно загрузить и получить тот же результат.
6. Индекс связей обеспечивает **полный** поиск обратных связей по всей БД за доли секунды.
7. Граф связей корректно исключает возвратные рёбра и соблюдает ограничения глубины по sd/su.
8. Уровни узлов корректно включают отрицательные значения (обратные связи от корня).
9. Транзитные JOIN автоматически включаются для формирования полных путей к включённым таблицам.
